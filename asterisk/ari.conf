; VoiceShield — ARI user (Asterisk 20 LTS).
;
; The password lives ONLY in ari-secrets.conf, generated locally by
; scripts/setup-asterisk-wsl2.sh (0600, never committed). This file
; contains NO credentials. Mirror the same password into the gateway
; environment as VFD_ASTERISK_ARI_PASSWORD (never in git).

[general]
enabled = yes
pretty = yes
allowed_origins = *

#include "ari-secrets.conf"
